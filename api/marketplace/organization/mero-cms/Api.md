# Mero CMS - API

## Base Path
`/api/cms`

## Endpoints
- `GET /pages`: List pages for the active tenant.
- `POST /pages/publish`: Deploy changes to the live site.
- `GET /media`: Access the tenant's media library.
- `POST /forms/submit`: Capture external form data (public endpoint).
