[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install --prefix api --legacy-peer-deps']

[phases.build]
cmds = ['npm run build --prefix api']

[start]
cmd = 'node api/dist/main.js'
